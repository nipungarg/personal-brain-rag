# Copy to .env and set values. .env is the source; config.py loads it and exposes settings.
# Do not commit .env (it may contain secrets).

# --- Required (secrets) ---
OPENAI_API_KEY= your_openai_api_key

# --- Optional: override defaults (non-secret config) ---

# Models
# EMBEDDING_MODEL=text-embedding-3-small
# LLM_MODEL=gpt-4o-mini

# Retrieval
# DEFAULT_TOP_K=4
# RELEVANCE_MAX_DISTANCE=1.5
# RRF_K=60
# RERANKER_MODEL=cross-encoder/ms-marco-MiniLM-L6-v2
# RERANK_K=20
# CACHE_SIM_THRESHOLD=0.90
# MIN_SIMILARITY=0.5

# Backend
# DEFAULT_BACKEND=chroma
# DEFAULT_COLLECTION=documents

# Paths (relative to project root if not absolute)
# DATA_DIR=data
# CHROMA_PERSIST_DIR=chroma_db
# FAISS_DB=faiss_db
# LOG_FILE=eval/run_log.jsonl

# Chunking
# CHUNK_SIZE=500
# CHUNK_OVERLAP=100

# Runtime
# MAX_TOKENS=1000
# TEMPERATURE=0.2
# INPUT_PRICE_PER_1M=0.15
# OUTPUT_PRICE_PER_1M=0.60
# LOG_LEVEL=INFO

# Eval
# EVAL_TOP_K=4
# EVAL_TEMPERATURE=0.0

# Server (long-lived service)
# SERVER_HOST=0.0.0.0
# SERVER_PORT=7860
